{
  "name": "@hadash/beakpeek-service",
  "version": "0.1.0",
  "private": true,
  "description": "Modern feeder intelligence service for HADash.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/main.mjs",
    "dev": "node --watch src/main.mjs",
    "setup:python": "sh scripts/setup-python.sh",
    "check": "node --check src/main.mjs && node --check src/server.mjs && node --check src/service.mjs",
    "test": "node --test"
  },
  "optionalDependencies": {
    "mqtt": "^5.13.1"
  }
}
